# D:\polyglot_connect\.github\workflows\deploy.yml
name: Build and Deploy Polyglot Connect App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies (including dotenv-cli) ⚙️
        run: |
          npm ci 
          npm install -g dotenv-cli

      - name: Create .env file for Vite build from Secrets 🔑
        run: |
          echo "VITE_GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" > .env
          echo "VITE_GEMINI_API_KEY_ALT=${{ secrets.GEMINI_API_KEY_ALT }}" >> .env
          echo "VITE_GEMINI_API_KEY_ALT_2=${{ secrets.GEMINI_API_KEY_ALT_2 }}" >> .env
          echo "VITE_GROQ_API_KEY=${{ secrets.GROQ_API_KEY }}" >> .env
          echo "VITE_TOGETHER_API_KEY=${{ secrets.TOGETHER_API_KEY }}" >> .env
          echo "VITE_TEST_VAR=HelloFromActionBuildWithDotenvCli" >> .env
          echo "Action .env file created. Contents:"
          cat .env 

      - name: Build with Vite using dotenv-cli 🛠️
        run: dotenv -e .env -- npm run build 

      - name: Verify Build Output (Check if VITE_TEST_VAR was injected) 🔍
        run: |
          echo "Listing contents of ./dist/ directory:"
          ls -R ./dist
          JS_BUNDLE_FILE=$(find ./dist/assets -name "index-*.js" -o -name "main-*.js" | head -n 1)
          if [ -n "$JS_BUNDLE_FILE" ]; then
            echo "Checking bundle: $JS_BUNDLE_FILE"
            if grep -q "HelloFromActionBuildWithDotenvCli" "$JS_BUNDLE_FILE"; then
              echo "✅ VITE_TEST_VAR value was correctly injected into the JS bundle."
            else
              echo "❌ VITE_TEST_VAR value was NOT found in the JS bundle."
            fi
          else
            echo "Could not find main JS bundle in dist/assets/ to verify."
          fi

      - name: Deploy to gh-pages branch 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist 
          force_orphan: true 
          keep_files: false